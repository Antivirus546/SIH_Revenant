# UrbanHeat AI — simulator-optimizer subsystem dependencies
# Install:  pip install -r requirements.txt

# Core (required for heuristic mode + optimizer)
numpy>=1.24
pandas>=1.5

# Optional — enable ML simulation mode.
# Without these, create_ml_simulator() automatically falls back to
# heuristic mode (documented behaviour; nothing crashes).
scikit-learn>=1.2
xgboost>=1.7
Converting satellite imagery into a spreadsheet involves a geographic processing technique called Zonal Statistics.

Here is exactly how the pipeline transforms the visual data into the tabular format needed for your machine learning model:

### 1. Generating the Master Grid

* First, we take the bounding box for Shivajinagar and overlay a mathematical "fishnet" across it.
* This creates a blank map layer made entirely of 100m × 100m vector squares (polygons).
* Every square is assigned a unique ID number, which serves as the primary key (the rows) for your tabular dataset.

### 2. Extracting Image Data (Zonal Statistics)

* To convert the Landsat and Sentinel-2 images into numbers, we use Zonal Statistics.
* The script overlays the 100m grid onto the satellite image pixels.
* It then calculates a summary statistic—such as the mean or median of all the pixel values—that fall within the geometry of each specific grid square.
* The raw image is discarded, and the resulting statistic (like the average temperature) is saved as a numerical column for that grid square.

### 3. Extracting Vector Data (OpenStreetMap)

* We perform a similar overlay process for the OpenStreetMap roads and buildings.
* Using spatial joins, the script calculates physical metrics for each individual grid square.
* This translates the map shapes into tabular features, such as calculating the total length of roads or the percentage of area covered by buildings per square.

### 4. The Final Tabular Merge

* Finally, all of these separate calculations are merged together using the unique Grid IDs.
* The final product is a standard CSV or GeoJSON table where each row is a 100m patch of land, and the columns are the environmental and infrastructural features.
* Because XGBoost only understands rows and columns, this tabular format is exactly what Darshith needs to train the model.

Would you like me to outline the logic for building this 100m fishnet grid next, or should we discuss how Darshith will evaluate the model's accuracy?